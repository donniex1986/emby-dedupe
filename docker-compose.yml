services:
  emby-dedupe-app:
    build: .
    container_name: emby-dedupe-app
    ports:
      - "5055:5000"
    environment:
      - SECRET_KEY=change-this-secret-key
      - DB_PATH=/app/data/app.db
    volumes:
      - /mnt/user/appdata/emby-dedepe/data:/data
    restart: unless-stopped
